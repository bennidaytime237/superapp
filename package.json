{
  "name": "sage",
  "version": "1.0.0",
  "private": true,
  "description": "Unified crypto superapp — Swap (Uniswap), Bridge (Across), Buy/Sell (MoonPay), Gas top-up",
  "scripts": {
    "build": "echo 'Static site — no build step required'"
  }
}
