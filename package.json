{
  "name": "sage",
  "version": "1.0.0",
  "private": true,
  "description": "Unified crypto superapp — Swap (Uniswap), Bridge (Across), Buy/Sell (MoonPay), Gas top-up",
  "scripts": {
    "build": "npx tailwindcss -i tailwind.css -o public/tailwind.output.css --minify"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.19"
  }
}
